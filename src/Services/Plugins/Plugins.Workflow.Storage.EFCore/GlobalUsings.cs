// The storage plugin maps records / definitions / status constants from the full Abstractions
// surface. Same rationale as the Engine plugin — globals avoid five-line using blocks per file.
// Root namespace gives access to plugin-wide conventions (e.g. WorkflowJsonOptions).


